# 审查与范围更正

2026-09-12；同家族新上下文只读审查，WARN / provisional，非独立科学验证。

1. 原协议truth_read=false表述不准确。执行版整体读取了内部matched_windows.csv，该表含人工计数列；仅使用video_id/cohort进行成员核对，未发现预测依赖计数。故应表述为“内部参考字段进入内存，但未用于预测或评分”，不是进程级参考隔离。该问题不涉及久福参考。未来runner已改为usecols仅解析ID和cohort；本次执行快照与原协议保留，不能追溯改称完全未读取。
2. 本次v1为固定半径20提取与冻结信号函数，不是完整自适应ROI主线。冻结method_config另有16–24像素的roi_policy；追加独立adaptive_roi_v1结果应用该策略，不覆盖固定半径产物。
3. 时间长间隔拒绝不等于信号可观察性门控。旧repair_missing信号函数可能对较长鼻孔缺失插值；例如已生成170333存在20个连续网格样本的双侧温度缺失。不能将数值输出当作全程真实观测，暂不部署。追加信号缺失描述，不事后按计数误差调门限。
4. 模型/代码文件哈希不等于冻结完整运行环境。实际YOLO参数在运行结束保存，依赖与模块位置应另记；不声称跨环境逐位可重复。

原始窗口取已有锚点帧的顺序解码时间为起点，取[0,30)；这不是对最初要求的绝对拍摄时刻或旧CFR参考范围的验证。无人工参考评分，无新R²，无正式久福预测。

## 后续处置（2026-09-14）

- 审查后补充：自适应阶段检查与无计数原始审计清单的49个ID、源路径、源哈希、起始帧，核对源协议、30秒时长和采样策略；不再仅凭49行就认可来源。
- 温度行、帧名、哈希列表及时间映射要求顺序和长度完全一致；重算时间映射核对。时间拒绝不能被残留温度文件覆盖，缺少必要文件立即报错。
- 固定分支完整49窗已完成：37数值输出、9时间长间隔、2末端不足、1无有效信号。9657个预测网格帧及895个算法峰的一致性检查通过，12个冻结文件哈希未变。不是准确率证据。
- 核验脚本首次读取含空值的次数列时遇到26.0字符串，改为验证浮点数为整数；时间CSV默认解析另使近等距选择改变一个索引，改为float_precision=round_trip后严格重放一致。没有更改已保存预测或放宽离散索引比较。
- 自适应初次运行中文路径读取失败，原函数将读取失败跳过，导致全空温度表。该轮adaptive_roi_v1无效，详见其INVALID_RUN.md，不能按0/49覆盖报告。
- 重跑以局部I/O适配器替换半径表函数的图片读取依赖，使用numpy.fromfile和OpenCV imdecode保持BGR；不修改冻结源码、半径公式或RF。读取失败立即抛错。结果另存adaptive_roi_20260914_v2，不能覆盖无效原件。新运行是否完成和核验通过以该目录产物为准。
- 本文件记录同家族审查发现及执行者处置，不把处置说明冒充新的独立审查PASS。
