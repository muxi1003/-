# 不可采用的运行

2026-09-14补录：本轮于2026-09-12运行时，冻结半径表函数直接调用OpenCV imread，中文路径读取失败，但函数继续执行并留下全空温度表。因此38个no_valid_temperature_signal是I/O失败产物，不是鼻孔可观察性结论。其0/49输出不得报告为模型性能或有效覆盖。

保留全部原件，不采用、不覆盖。后续使用显式支持中文路径的BGR读取适配器，读取失败立即报错；重跑结果独立保存为adaptive_roi_20260914_v2。固定半径原始运行及其已通过的核验不受本轮失败影响。
