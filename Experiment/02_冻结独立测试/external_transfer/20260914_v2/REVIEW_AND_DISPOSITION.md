# 补充审查与处置

日期：2026-09-14。审查者：Lovelace；保证级别same-family provisional。原审查结论FAIL，不将其改写为PASS；也不等于发现伪造。

| 发现 | 本次处置 | 未作出的宣称 |
|---|---|---|
| 方法路径未与release路径直接比较 | 新增独立验收脚本，在评分前强制核对实际方法路径、文件摘要及实际导入的信号模块 | 不声称原运行器对任意篡改均安全 |
| 包版本仅记录，恢复未复核 | 新增验收脚本核对Python、可执行文件、平台及全部登记版本。本次运行不中途更换环境，FFmpeg另装Experiment/tools_deps，不改plant_gpu依赖 | 当前版本相同不是独立的历史进程取证 |
| 最终CSV写后中断无法自动续封 | 本次保持同一运行直到完成；若发生中断，保留原产物，验证一致性后独立恢复，不覆盖原锁或混入别的方法 | 不声称已有通用事务恢复机制 |
| 窗口外下一帧较晚会触发保守间隔拒绝 | 保留原已锁定规则，可能过度拒绝的限制写入外测报告，不根据外测输出或真值放宽 | 拒绝不代表必然完全不可观察 |
| OpenCV read失败和EOF未区分 | 额外验收要求每窗实际解码达到30秒边界，逐帧时间与此前无结果preflight一致，并重新核对全部原视频SHA | 不声称OpenCV错误分类或独立解码器已验证 |
| 旧内部ROI适配输入表未先验封存 | 保留该内部验证证据限制；本次外测同进程从原图生成base后直接在内存进入ROI，所有阶段产物逐窗封存，验收重放峰 | 不将旧表事后哈希冒称最初生成时间证明 |

程序验收PASS只指确定性工程检查通过，不能升级为独立科学审查PASS、测温准确、解剖可见或事件参考可信。审查者曾意外检索到历史结果摘要，已排除，不声称严格结果盲审。

外测算法参数、RF权重、YOLO权重、30秒成员、采样规则与人工计数均不因本次审查而调整。新增验收和计数评分在evaluation_plan_lock.json登记后执行；评分前必须先封存全部271窗预测。
