# 同家族只读审查

日期：2026-09-14。结论WARN / same-family / provisional，不是独立科学复现或正式外测通过。

已核实：推理入口未使用参考计数，融合truth_row=None，参考读取在另一评分命令；候选只能保持旧计数或整窗留空，没有将诊断删峰后的部分次数当作完整计数。内部六行R²/MAE复算吻合；同输出子集预测完全相同；raw49为9输出/40弃权，不与旧参考评分。12个冻结条目、两份预测seal、49份温度快照及检查时的代码副本一致。

剩余限制：

1. score入口仅检查内部预测seal；报告核验另检查了raw seal与参考交付哈希，但没有将来源表、评估脚本及共享指标实现全部纳入预测前seal。当前一致不等于完整上下文已不可变封存，也不是可信外部预注册。
2. raw分支依赖N048上游时间契约；本层只核对成员和计数回放并记录温度哈希，不能凭本实验独立证明原始物理时间绑定。支持表中时间按30秒均匀网格生成，不是新增解码时钟证据。
3. detected、置信度与有限坐标/温度只是来源支持代理，不能证明鼻孔解剖定位正确、无运动污染或事件真实。混合模式要求双侧直接支持可能过严。

采用决定：保留为诊断候选，默认不改。不得声称准确率提高、55个标记都是伪峰、真实可见性得到验证或外部泛化已通过。上述封存与raw绑定限制在正式释放推理器前仍须补强；本次不是正式释放。
