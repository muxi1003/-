# 快捷键输入法兼容修复

2026-09-15：用户反馈只有空格有效。旧代码在模拟Windows输入法的key=Process、code=KeyQ事件下未记录事件；对应回归测试先失败，再修复通过。这是可复现的兼容问题，不代表已直接检查用户的输入法。

原页面及模板改为优先识别event.code的物理按键。非编辑区域支持输入法组词状态；输入框、下拉框和可编辑文本仍禁止触发快捷键。保留组合键、长按保护及原字段验证。标签页版本为20260915.2，地址和缓存键不变。

- index_before.html：本次修复前页面备份。
- ime_validation.json：真实视频及模拟IME形态键盘事件的测试结果。
- keyboard_validation.json：普通快捷键完整回归结果，包含数据不变检查。

对应脚本为上两级的test_event_keyboard_ime.cjs及test_event_keyboard.cjs。测试在隔离Edge浏览器完成，没有改写用户人工标注CSV或活动浏览器缓存。历史v1结果和20260914交付哈希保持不变，新页面哈希在本目录验证结果中登记。
