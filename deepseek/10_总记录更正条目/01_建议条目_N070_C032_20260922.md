# 建议追加到 `docs/cow_rr_project_log.md` 的更正条目

- **日期**：2026-09-22，Asia/Shanghai（节点 `D011` 任务 B）
- **说明**：本文件**本身不是项目总记录**，只是供你粘贴的条目草稿。
  我没有修改 `docs/cow_rr_project_log.md`（那是 Codex 侧唯一总账）。
- **编号衔接**：总记录当前最新节点 **N069**、最新更改 **C031** → 本稿使用 **N070 / C032**。
- **请在粘贴前核对编号是否仍未被占用。**

---

## 一、追加到 §4 末尾的新节点

```markdown
### N070 B73-P 真值调整的确认、污染范围排查与旧研究线补录

- 日期：2026-09-22，Asia/Shanghai；来源：用户说明 + 本次只读核查（DeepSeek 侧，未改项目文件）。
- 需求与原因：用户说明 B73-P 的 R²=0.946595 是"根据算法预测结果调整了几个呼吸频率"后得到；
  需要确认该说明是否成立、影响范围多大、以及历史文档应如何标注。
- 数据和基线：73 视频集；`innovation_repro_summary.csv`（未调整真值，R²=0.928992）与
  `paper_repro_summary.csv`（调整后真值，R²=0.946595）为同一批 73 视频的两套真值。
- 执行动作（全部只读，未修改任何项目文件）：
  - 比对两份 summary 的 `truth_count`，定位差异视频；
  - 以 4 个差异视频为指纹，扫描 `Dataset_new/72video`、`Dataset_new/external_al_images_single_reference`
    与 `Experiment/` 下的 CSV，判定各文件继承的是哪一套真值；
  - 复核旧研究线（safe gate / 残差校正 / 校准无关）所用真值。
- 改动位置：本节点仅登记，未改任何数据、算法、模型或人工计数。
- 结果与验证：
  1. **确认调整存在**：两份 summary 的 `truth_count` 恰好 4 个视频不同——
     `210994`（7→6）、`211049`（4→5）、`ns196527`（10→11）、`ns211117`（10→11），
     **四个全部被改成与算法预测完全一致，误差全部归零**。
  2. **调整写入源表**：`Dataset_new/72video/video/temperature_curves.csv`（73 视频参考清单，D02）
     与 `paper_repro_summary.csv` 均为调整后版本，上游来源为前者。
  3. **污染范围**：扫描命中 110 个含计数列且涉及 4 个目标视频的 CSV，
     其中 **34 个取调整值（ADJUSTED）**、**76 个取原始值（clean）**、**0 个混杂**。
     即调整是一次性写入源表后向下游派生。
  4. **`Experiment/01_同口径消融/runs/20260909_v1/paired_predictions.csv` 部分受影响**：
     其 `internal73` 的 32 行（4 视频 × 8 变体）全部取调整值；
     其 `anchored49` 使用 49 窗人工计数（另一套参考），**不受影响**。
     → 因此 **E040 的 73 列结果建立在调整真值上**；**N035/N046/N048–N061 等 49/47 窗结论不受影响**。
  5. **旧研究线（2026-07 那批）为 clean**：`paper_repro_signal_aware_safe_policy_*`、
     `paper_repro_quality_residual_*`、`paper_repro_signal_consensus_*`、
     `paper_calibration_free_*`（P2g 相关）、`paper_meta*` 等，其真值与
     `innovation_repro_summary.csv` **0 处不同**。
  6. 附带解释 C033（见下）：49 窗表 `short_clip_reference_rr_bpm_not_window_truth` 与 73 表 `rr`
     的 2 行不一致中，`ns196527` 正是本次调整的 4 个视频之一（旧计数 10→新 11，66.7→73.3），
     属同一次调整未在两表同步，**不是两个并行版本**。
- 采用决定：B73-P（0.946595）**不得作为任何评估基线或论文结果**；
  该口径的诚实结果以 `innovation_repro_summary.csv` 的 **R²=0.928992、MAE=1.9713、exact 49/73** 为准。
  历史文件一律保留、不修改，仅在此登记适用范围。
- 备注/待更改：调整事实由用户主动说明提供；本次为第三方只读核查，未播放视频、未改人工计数。
  另一处 49 窗表与 73 表的短片参考差异（`bs170227`，33.0 vs 40.0）**不属本次调整**，成因仍待追溯。
- 后续：T15（把旧研究线补进总记录）；引用 73 集结果前先确认文件的真值版本；关联 C032/C033。
```

---

## 二、追加到 §6.1 更改台账的行

```markdown
| C032 | 2026-09-22 | N070、B73-P、§3.2 | B73-P 的 R²=0.946595 被当作"当前已保存指标口径"，其真值来源未被登记 | 确认该 R² 建立在被人工调整过的真值上：4 个视频的计数被改成与算法预测完全一致。登记为不可作为基线的历史快照，诚实口径为 0.928992 | 已登记；历史文件未改；引用前需确认真值版本 |
| C033 | 2026-09-22 | N070、C011、D07 | 49 窗表 `short_clip_reference_rr_bpm_not_window_truth` 与 73 表 `rr` 有 2 行不一致 | 其中 `ns196527` 由 C032 的同一调整造成（旧计数 10→11）；`bs170227` 成因待追溯。不是两个并行参考版本 | 部分澄清；`bs170227` 待查 |
```

---

## 三、建议修改 §1.2「不得混淆的结论」

原第 1 条：

> 1. `innovation_repro` 的 73 视频 R²=0.928992 与当前 `paper_repro_metrics.csv` 中的 R²=0.946595 属于不同结果快照。后续用户指定哪套基线，就与那套冻结真值、时长和预测比较，不能只选较大的数字。

**建议改为**（保留原意，补上真值差异的实质）：

> 1. `innovation_repro` 的 73 视频 R²=0.928992 与 `paper_repro_metrics.csv` 中的 R²=0.946595 属于不同结果快照。**后者（B73-P）使用被调整过的真值——4 个视频的计数被改成与算法预测完全一致，因此不得作为任何基线或论文结果**；该口径的诚实结果是 0.928992。后续用户指定哪套基线，就与那套冻结真值、时长和预测比较，不能只选较大的数字。见 N070/C032。

**建议新增第 11 条**：

> 11. **同一目录下存在两套 73 集真值**（`innovation_repro_summary.csv` 为原始冻结版，`paper_repro_summary.csv` 与 `Dataset_new/72video/video/temperature_curves.csv` 为调整版）。引用 73 集任何结果前，必须先确认该文件继承的是哪一套；已确认继承调整版的 34 个文件见 N070 与 `deepseek/09_真值污染排查/`。不得以同名新文件覆盖旧真值。

---

## 四、建议修改 §3.2 指标台账的 B73-P 行

原行：

```markdown
| B73-P | `paper_repro`，当前已保存指标口径 | 73 | 0.946595 | 1.691728 | 51/73 | 96.6721% | [paper_repro_metrics.csv][paper_metrics] |
```

**建议改为**：

```markdown
| ~~B73-P~~ | ~~`paper_repro`，当前已保存指标口径~~ **已作废：使用调整真值，不得作基线**（N070/C032） | 73 | ~~0.946595~~ | ~~1.691728~~ | ~~51/73~~ | ~~96.6721%~~ | 见 N070 |
```

并在 §3.2 表格下方补一句：

> **B73-I 的 0.928992 才是 73 短片口径的诚实基线**；B73-P 因使用调整真值（4 个视频计数被改为与预测一致）已作废，仅保留供追溯（N070/C032）。

---

## 五、建议更新页首字段

| 字段 | 建议值 |
|---|---|
| 本文位置 / 建立日期 | 不变 |
| 最近更新 | `2026-09-22，Asia/Shanghai` |
| 文档版本 | `v1.40，B73-P 真值调整确认与污染范围排查` |
| 最新节点 / 下一编号 | `N070 / N071` |
| 当前阶段 | 追加：**已确认 B73-P 使用调整真值并作废；污染范围 34 个受限 / 76 个 clean；旧研究线（safe gate / 残差校正 / P2g）为 clean** |

---

## 六、需要你决定的一处

我在 §3.2 建议把 B73-P 行**划掉保留**（不删除），以符合总记录"保留历史、另记更改"的规则。
若你希望保留数字但加更强的警示标记，也可以改成"⚠️ 作废"前缀而不划删除线——请按你的偏好定。

---

## 七、本稿依据

| 内容 | 证据 |
|---|---|
| 4 个视频的真值差异 | `innovation_repro_summary.csv` vs `paper_repro_summary.csv` 的 `truth_count` |
| 调整方向（全部改为与预测一致） | 同上，逐视频比对 `peaks` 与 `truth_count` |
| 污染范围 34 / 76 / 0 | `deepseek/09_真值污染排查/scan_contaminated_truth.py` 运行输出 |
| 旧研究线为 clean | `paper_repro_signal_aware_safe_policy_predictions.csv` 与 `paper_repro_quality_residual_predictions.csv` 的 `truth_count` vs innovation 真值 0 处不同 |
