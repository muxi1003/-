# DeepSeek 侧工作日志

- **用途**：记录 DeepSeek 助手在本项目中执行的每一个有意义的工作节点。
- **编号**：`D###`（与项目总记录的 `N`/`C`/`T` 编号区分，避免冲突）。
- **规则**：只追加、不覆盖；修改旧结论时保留原条目并新增条目说明旧值与新值。
- **不替代**：`docs/cow_rr_project_log.md` 仍是唯一项目总记录。

---

## D001 建立 DeepSeek 工作目录、通读项目并完成过程梳理

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户直接指令 + 用户提供的项目总记录副本与论文初稿 PDF
- **需求**：
  1. 今后在本项目生成的更改文件统一保存在 `E:/real/use_code/yoloV8/deepseek`，同一内容文件放同一文件夹；
  2. 编写一份日志文本文件；
  3. 先通读并梳理项目全过程。
- **实际动作**：
  1. 通读 `docs/cow_rr_project_log.md` 全文（1124 行 / 186,178 字节，与用户提供副本 sha256 `37ee0efd…` 一致）；
  2. 通读论文初稿 `Experiment/04_跨牧场机制补强/20260921_r3_v1/manuscript/中文论文初稿.md`（340 行）；
  3. 核验用户提供的初稿 PDF 身份；
  4. 建立 `deepseek/` 目录结构与编号约定；
  5. 撰写项目过程梳理文档。
- **改动文件（全部为新增）**：

| 路径 | 说明 |
|---|---|
| `deepseek/README.md` | 目录用途、组织约定与 `D###` 编号规则 |
| `deepseek/deepseek_log.md` | 本文件 |
| `deepseek/01_项目过程梳理/项目过程梳理_20260922.md` | 项目全过程梳理（12 节） |
| `deepseek/02_初稿PDF核对/00_初稿PDF身份核验_20260922.md` | 初稿 PDF 身份核验与后续核对入口 |

- **结果与验证**：
  - 用户提供的 PDF（1,130,818 字节）sha256 = `e3ded1250ae46e42be5c113e5dffcdfe8219bc4c28d1e26be91d9af608e5842a`；
  - 项目内 `Experiment/04_跨牧场机制补强/20260921_r3_v1/manuscript/奶牛热红外呼吸检测_中文初稿_20260921.pdf` 哈希**完全相同** → 判定为同一文件，项目内已有 Markdown 源文，无需从 PDF 反向提取；
  - 环境核验：系统默认 Python（3.13.6）无 pypdf/pdfplumber/PyMuPDF，`pip install` 在当前沙箱下超时未完成；`E:/real/anaconda/envs/plant_gpu/python.exe` 已具备 `fitz`(PyMuPDF) 与 `PyPDF2`，后续如需 PDF 解析优先使用该解释器，**不新建环境**；
  - 项目内 Markdown 源文为 **GB18030 编码**，用 PowerShell `Get-Content` 读取会乱码，须用支持编码识别的读取方式（本次用 `read` 工具）。
- **采用决定**：本次全部为**文档性工作**，未修改算法、模型、人工计数、历史指标、导航或任何实验产物；未新增实验、未重跑推理、未联网检索。
- **备注/待更改**：
  - 梳理文档性质为**二手归纳**，未重新核验原始产物哈希（除初稿 PDF 外）；如与项目总记录冲突，以总记录为准。
  - `deepseek/.pylibs` 为尝试安装 pypdf 时创建的空目录，已清理。
- **后续**：
  - 待办：按用户后续指令核对论文初稿与项目记录/指标的一致性（见 `02_初稿PDF核对/`）；
  - 关联项目总记录待办：T10（投稿前证据补强）、T13（R3 相位与可见性收束）、T14（定位/ROI 可观测性）。

---

## D002 初稿数字核对：9 表 + 正文，未发现快照混用，发现基线公平性问题

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户指令"按你列顺序进行"（第 ① 项）
- **需求**：把论文初稿的每个数字与项目总记录 §3 指标台账逐项对齐，确认基线 ID、样本数、真值/时长版本一致。
- **实际动作**：逐表核对初稿 9 张表与正文数字，与总记录 §3.2/§3.6/§3.7/§3.8/§3.9/§3.10 及 N058/N059/N060 比对。
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/02_初稿PDF核对/01_初稿数字核对_20260922.md` | 新增，逐表核对结果 |

- **结果与验证**：
  - **未发现快照混用**：B73-I/B73-P 未混用；216 输出与 216 完整参考已正确区分；E051（122 窗）与 E055（173 窗）未混用；R3 参考敏感性未写成算法提升；缓存层对照未写成端到端净效应。**初稿在防止数字混用上是合格的**。
  - 表格中凡总记录收录的数字**全部一致**（表1/表2 四行/表3 核心/表6 全部/表8 全部/表9 全部）。
  - **约 30+ 个数字总记录查无**，集中在：表2 的 3 行、表3 的 RMSE、表4 全部 6 行、表5 第 3 行、表7 林甸原观看时间、以及 §4.2/§4.3/§4.4/§4.6/§4.8 的区间与分布统计。已逐项列出应回溯的原始产物路径。
  - 🔴 **发现基线公平性问题**：初稿用作"原路线"的 F0G0P0 = 0.223799，比项目自身复现（r20 固定 39 窗 0.902657、B73-P 0.946595）低 0.65–0.72。按初稿 §3.3，P=0 使用 `distance=6/prominence=0.05` 简易峰规则，故 F0G0P0 **不是原文方法的忠实复现**，而是劣化配置。
  - 🔴 由此，全文**缺少"同窗同参考下的原文方法对照"**——正是总记录 N039 列出的首要缺项。
  - 🟡 附带发现（DeepSeek 推断，项目记录未见）：F0 对峰规则敏感（0.22→0.60），F1 几乎不敏感（0.83→0.89），提示 F 的价值在**稳健性**而非提升幅度。
- **采用决定**：本次为**只读核对**，未修改初稿、未改任何数据或结论。所有"查无"项标注为**待核实**而非错误。
- **备注/待更改**：核实 §4.4 的"内部 9 / 久福 38 个总数一致窗"时注意与 N054 的"R1/R2 共同完整计数窗 39/151"区分，两者是不同比较。
- **后续**：① 的三个待办中第 2 项（审计 2518 份 JSON）与第 3 项（观察者内一致性）尚未开始。

---

## D003 投稿策略评估：初稿立意与基线问题，给出重排方案

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户提出"精度不看好、创新点没成立、GPT 初稿立意与参考文献不同"的疑问
- **需求**：给出诚实的可行性判断与改进路线。
- **实际动作**：基于 D002 核对结果、总记录台账与初稿全文，做立意/创新点/证据链诊断，提出三方案并推荐。
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/03_投稿策略/投稿策略评估_20260922.md` | 新增，策略评估与重排建议 |

- **核心结论**：
  1. 用户判断中"立意不对""创新点不成立"**成立**；"精度很差"**需纠正**——内部 R²=0.891 与原文 0.92 同级，差的是跨牧场 0.420，而初稿把它当成了全文主角。
  2. 创新点三缺一：**G 已被自身消融否定**；**P 是规则包**；F 半成立。
  3. 建议贡献重排为三条：F 的**稳健性**（非提升幅度）、**事件级评价协议**（4345 事件证明计数指标掩盖事件错误）、跨场失效**分层归因**。
  4. 补实验优先级：① 原文方法忠实复现对照（必需）→ ② F 稳健性配对统计（极低成本）→ ③ 观察者内一致性（低成本，即原第 ③ 项）。
- **采用决定**：**建议性文档，不构成项目结论**；未修改初稿、算法、数据或任何采用决定。
- **备注/待更改**：§4 的"稳健性"表述为**推断**，采用前必须用原始逐窗预测做配对统计验证；§7 的"哪个配置忠实对应原文方法"是**待用户或代码确认**的开放问题。
- **后续**：等用户决定是否按方案 B/C 重排；① 的第 2、3 项待推进。

---

## D004 r20 固定确认为原文基线后：对照核算显示"优于原文"主张不成立

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户答复「r20 固定」（回应 D003 §7 的开放问题）
- **需求**：以 r20 固定为原文忠实基线，核算"对比优势"是否成立。
- **实际动作**：只读读取并比对 4 份已保存结果，未新算模型、未改任何产物：
  `lindian_adaptive_roi_metrics.csv`、`Experiment/01_同口径消融/runs/20260909_v1/` 的 `metrics.csv`、`paired_vs_control.csv`、`RESULTS.md`。
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/03_投稿策略/02_r20基线对照核查_20260922.md` | 新增，核心对照核算 |
| `deepseek/03_投稿策略/投稿策略评估_20260922.md` | 顶部追加 D004 更新提示（保留原文，不覆盖） |

- **结果与验证**（同一批 49/39 窗，均 r20 输入）：

| 方法 | R² (49) | R² (39) | 完全计数 (49) | MAE (49) |
|---|---:|---:|---:|---:|
| **r20 固定（原文忠实复现）** | **0.877195** | **0.902657** | **15/49** | **2.7338** |
| 自适应 ROI（项目默认 B49-MA/MP） | 0.888037 | 0.913337 | 14/49 | 2.6112 |
| F1G0P1 | 0.880944 | 0.930344 | 13/49 | 2.8158 |
| **F1G1P1（初稿主线）** | **0.876482** | 0.925362 | 13/49 | 2.8566 |

  - ΔR² vs r20 固定：F1G1P1 = **−0.0007 (49)** / +0.0227 (39)；F1G0P1 = +0.0037 / +0.0277。
  - 49 窗全量上，本文主线的 R² 略降、MAE 变差 0.1228、完全计数由 15 降至 13。
  - 初稿"0.223799 → 0.834292"的效应量来自 F0G0P0 这一**被削弱的对照**；换用真实基线后增量近乎归零。
  - 另发现：r20 固定（0.9027/39）**高于** F0G0P1（0.7749/39），说明**消融的 F0 ≠ 原文方法**，原文另有其融合与峰值处理。
  - 一并发现总记录 §3.5 标注笔误：`E040-49-QP-A` / `E040-49-QP-P` 写为 "F1G1P1"，按 QP=F1G0P1 定义应为 **F1G0P1**（数值 0.880944 / 0.930344 与 CSV 一致，属标签错，非数据错）。
- **采用决定**：本次为**只读核算**；未修改项目总记录、初稿、算法、模型或任何采用决定。原 D003 的"方案 B（重排为方法论文）"可行性下调，已在原文顶部标注更新。
- **备注/待更改**（三条硬约束，后续写作必须遵守）：
  1. **不得再以 F0G0P0 作为原文基线**；r20 固定才是。
  2. §2 对照的 39/49 用的是较早参考版本，与初稿的 47 个 R2 完整参考窗**不是同一版本**，差值**不能直接当作最终结论**。
  3. 要写"对比优势"必须先查清 r20 固定的确切配置（融合模式 + 峰值规则参数），并**在统一参考上做配对 bootstrap**。
- **后续**：（1）只读查清 r20 固定配置（读 ROI 评估脚本与 `paper_repro_rr.py`）；（2）在 47 个 R2 完整窗上重算配对区间；（3）据结果决定论文骨架（方法论文 / 评价方法论文 / 降区）。

---

## D005 "R² 为什么在降低"归因：不是退化，是换了考题与标准答案

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户指出「B73-I 才是真正原方法；N031/N032 的 PPT 是方法上的完结，但后面不知道 R² 为什么在降低」
- **需求**：解释 R² 下降的原因。
- **实际动作**：只读比对 73 视频清单与 49 窗人工表，量化三种参考的差异并做 R² 分解；
  未重跑模型、未改任何项目数据文件。
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/04_R2下降归因/01_R2下降归因分析_20260922.md` | 新增，归因分析 |

- **结果与验证**：
  1. **73 集与 49 集是同一批源视频**：49 窗的 `video_id` **全部**属于 73 清单（49 个唯一 ID，全部命中）。
  2. 49 窗表含一列 `short_clip_reference_rr_bpm_not_window_truth`，即项目自己标注"短片参考不是本窗真值"。
  3. 对 39 个 completed 窗：**窗口参考 − 短片参考 = 均值 −4.138、中位 −4.063、绝对差中位 5.997、最大 31.934 bpm**；`corr = 0.8509`（r² = 0.7240）。
     极端例：`bs190624` 短片 7 秒 RR=86.0，30 秒窗 RR=54.1，差 **−31.9 bpm**。
  4. **R² 分解**（R² = 1 − MSE/Var）：
     - 73 短片 B73-P：MSE 9.039，Var 169.247 → R² 0.9466
     - 49 窗 B49-MA：MSE 14.350，Var 128.164 → R² 0.8880
     - 47 窗 F1G1P1：MSE 13.782，Var 126.325 → R² 0.8909
     MSE 升 59%（窗口变长、暴露更多运动与缺失，是真实差异）；Var 降 24%（参考变稳定，是"考题变窄"）。**两者同时压低 R²。**
  5. **每个集合内部 R² 是稳定或上升的**：73 集 0.929→0.947；49 集 0.877→0.888。**不存在退化，未发现 bug。**
  6. ~~**附带发现**：窗口表的短片参考列与 73 表的 `rr` 列**不一致**（39 窗中仅 2 个相同，最大差 7.0 bpm）→ 同一批视频的短片参考有两个版本，无记录说明关系。~~
     **⚠️ 本条已于 D006 更正：方向写反。正确结果是 39 窗中 37 个一致、仅 2 个不一致（`ns196527`、`bs170227`）。**
     该错误不影响 D005 主结论（R² 下降源于换集合/换参考），因其依据是窗口参考与短片参考的整体差异，与此两列是否一致无关。
- **采用决定**：只读归因，未修改项目总记录、初稿、算法、模型、人工计数或任何采用决定。
- **备注/待更改**（重要）：
  - **73 集的 0.929 / 0.947 不宜作为论文主指标**（建立在 6–20 秒短片 + 高离散度且与窗口真值仅 72% 方差重合的参考上）；**30 秒窗的 0.888 / 0.891 才是可辩护的数字**。
  - 若此前有人试图靠调 ROI 半径把 49 窗 R² 拉回 0.95，该目标**数学上不可达**（`lindian_adaptive_roi_metrics.csv` 中 22 种半径策略最好仅 0.888）。
  - D004 的对照结论**不受影响**（r20 固定与 F1G1P1 同在 30 秒窗、同参考口径）。
- **后续（待用户确认的两问）**：
  1. **"真正原方法"指 B73-I（0.928992，标签 innovation_repro）还是 B73-P（0.946595，标签 paper_repro）？** 按命名 paper_repro 才像原文方法复现，且 N031 记录 PPT 采用 B73-P；用户写的是 B73-I，与命名相反。**这决定"对比优势"跟谁比。**
  2. 短片参考的两列（§5）以哪个为准？

---

## D006 执行任务1和2：短片参考核对 + 统一 R2 参考配对对照（**终局结论**）

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户指令「先做 1 和 2」，并补充关键信息（见下）
- **用户本轮补充的重要事实**：
  1. 原文方法 = Chen 文献方法，原始代码目录 `E:\real\use_code\代码\站立呼吸\yoloV8`（**已确认存在**，含 `draw_curve`/`pre_data`/`runs`/`temperature_extraction`）；
  2. `paper_repro_rr.py` = GPT 依文献**复现**的版本；
  3. **B73-I = 在 `paper_repro_rr.py` 基础上做的改进**；
  4. **B73-P 的 0.946595 = 用户"根据算法预测结果调整了几个呼吸频率"后得到**；
  5. **没有一版忠于原文献方法**；要对比须回到 `temperature_extraction` + `draw_curve` 重跑。
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/05_基线对照/compare_r20_vs_method_on_R2.py` | 新增，可复现只读核对脚本 |
| `deepseek/05_基线对照/01_任务1和2结果_20260922.md` | 新增，任务1+2 结果 |
| `deepseek/04_R2下降归因/01_R2下降归因分析_20260922.md` | 更正 §5（见下） |

- **任务1结果（并更正 D005 错误）**：
  - **D005 写反了**：不是"仅 2 个相同"，而是 **39 窗中 37 个一致、仅 2 个不一致**。已在 D005 条目与归因文档 §5 更正。
  - 两处不一致：`ns196527`（49窗表 66.7 vs 73表 73.3，推断 49窗表该列仍是"计数10"旧版）、`bs170227`（33.0 vs 40.0，无法用当前 73 表字段复现，来源待追溯）。
  - 附带核实：73 表内部 `rr` 与 `60×count/duration` 一致；脚本报的"9/73 不一致"是**用 `round()` 对齐非整数 rr 的检查瑕疵**，非数据问题。
- **任务2结果（统一口径 = R2 20260917_v2 的 lindian49 complete 47 窗；9 条管线交集均恰为 47 窗）**：

| 方法 | R² | MAE | RMSE | 完全计数 |
|---|---:|---:|---:|---:|
| **r20 固定（原文候选基线）** | **0.872816** | 3.0200 | 4.0083 | **11/47** |
| F1G1P1（初稿主线） | 0.890902 | 2.9781 | 3.7124 | 9/47 |
| F1G0P1 | 0.894273 | 2.9355 | 3.6546 | 9/47 |
| F0G0P0 | 0.223799 | 5.3646 | 9.9022 | 14/47 |

  - **配对 bootstrap（2000 次，按窗重采样，seed 20260922）**：ΔMAE = 本文 − r20
    - F1G1P1 = **−0.0378**，95% CI `[−0.4248, +0.3409]` → **跨 0**
    - F1G0P1 = **−0.0814**，95% CI `[−0.4675, +0.2985]` → **跨 0**
  - **结论：在统一 47 窗 R2 参考上，本文方法与 r20 固定基线之间无任何统计上可辩护的差异**；R² 名义高 0.018–0.022，但**完全计数更少**（9/47 vs 11/47）。
  - **交叉验证**：算得 r20 固定 = **0.872816**，与初稿表7 第一行「r20 同配置，原观看时间 / 林甸 0.872816」**完全吻合** → 原文基线的数字**早已在初稿里**，只是被当作缓存层对照条件，从未被当作基线使用。
- **采用决定**：只读核算，未修改项目总记录、初稿、算法、模型、人工计数或任何采用决定。
- **⚠️ 需要严肃处理的新事项（B73-P 来源）**：~~用户确认 B73-P = 0.946595 是调整真值后的结果，不是评估结果。~~
  **🔴 本条已于 D007 更正并收回**：经逐行核查，`paper_repro_summary.csv` 的 `truth_count` 与 73 表 `breath_count` **73/73 完全一致**，
  **没有改真值的痕迹**。B73-P 的真实性质是"在开发集上调过峰规则"。详见 D007。
- **后续**：
  1. 在总记录登记 B73-P 来源（影响面最大）；
  2. 用 `temperature_extraction` + `draw_curve` 重跑**忠实原文基线**——这是唯一能支撑"对比优势"的路；
  3. 追溯 `bs170227` 短片参考 33.0 来源（影响面小）；
  4. 若第 2 项仍无差异（按 r20 对照推测很可能），论文须改立意。

---

## D007 B73 来源核查与 73 集结果谱系（**更正 D006**，并发现唯一正结果）

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户提供 Codex 会话截图（默认流程 0.928992 / 质量感知残差校正 5折CV 0.946624）
- **需求**：核清 B73-P 的真实来源。
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/06_B73来源核查/01_B73来源核查与结果谱系_20260922.md` | 新增 |
| `deepseek/05_基线对照/01_任务1和2结果_20260922.md` | 更正 §4.1 |

- **🔴 更正 D006**：D006 写"B73-P 是调整真值后的结果"，**该说法不成立，已收回**。
  核查证据：`paper_repro_summary.csv` 的 `truth_count` vs 73 表 `breath_count` = **73/73 完全一致**，`duration_seconds` 也 73/73 一致。
  → **没有改真值的痕迹**。D006 是我把用户"调整了几个呼吸频率"直接推断成"改真值"，属**超出证据的推断**。
- **B73-P 的真实来源（已精确复现）**：可由 `paper_repro_summary.csv` 完整复现
  （R²=0.946595 / MAE=1.691728 / RMSE=3.006423 / 51-73，**四项全中**）。
  该文件特征：`status=retuned` 73/73；`adaptive_peak_retuning=True` 73/73；
  `peak_retune_rule` = none 64 / dense_peak_count 6 / short_sparse_peak_count_edge 3；
  `retune_target_peaks` 仅 6 行有值且全为 10.0。
- **澄清（避免误判）**：`retune_target_peaks=10` 的 6 行真值也恰好是 10，
  但 `truth_count=10` 在 73 段中占 **44/73（60%）**，**不构成"抄真值"的证据**。
  真正的问题是项目总记录 §3.5 早已自述的"**73 的 P 含开发集专用目标峰数 10 规则**"——
  即**在评测集上开发并调参**，性质轻于改真值，且**项目自己已登记**。
- **73 集结果谱系（按诚实度）**：

| 方法 | R² | MAE | 完全计数 | 备注 |
|---|---:|---:|---:|---|
| 默认流程 = **B73-I** | **0.928992** | 1.9713 | 49/73 | 数值确认 = `baseline_default` |
| 残差校正 leave-one-prefix-group-out | 0.932500 | 1.8577 | 51/73 | 最接近换批次 |
| **残差校正 nested 阈值 CV** | **0.937336** | 1.7757 | 51/73 | **最诚实** |
| 残差校正 固定阈值 OOF | 0.942885 | 1.6187 | 53/73 | 较诚实 |
| 残差校正 阈值网格最佳 OOF | 0.945944 | 1.5017 | 55/73 | ⚠️ 全量挑阈值 |
| 去掉端点间隔（变体） | 0.946624 | 1.5388 | 54/73 | ⚠️ **截图引用的就是这个** |
| B73-P（`paper_repro_summary`） | 0.946595 | 1.6917 | 51/73 | ⚠️ 开发集调峰规则 |
| truth-calibrated 上限 | 0.999655 | 0.1389 | 73/73 | ⚠️ 文档已标注为上界分析 |

- **✅ 重要正面发现**：**"质量感知残差校正"是本项目目前唯一有正结果、且有交叉验证支撑的候选方法。**
  诚实口径下：nested CV **0.937336**（+0.0083）、group-out **0.932500**（+0.0035）、固定阈值 OOF **0.942885**（+0.0139）。
  `docs/thermal_rr_innovation_plan.md` 亦如实记录"有跨组潜力，但 bs 组下降、ns 组无改善"。
- **采用决定**：只读核算；未修改项目总记录、算法、模型、人工计数或任何采用决定。
- **后续（建议优先级）**：
  1. 把残差校正的诚实数字（nested CV / group-out）核实固化；
  2. **在 49/47 窗集上跑同一套残差校正** —— 若在 30 秒窗上也有 +0.01 级稳定提升，则同时解决"方法主张"与"数据集偏弱"两个问题；
  3. 在总记录登记 B73-P 的适用范围（"开发集调峰规则，不作基线"）；
  4. `bs170227` 短片参考来源追溯。

---

## D008 只读可行性评估：把质量感知残差校正迁移到 49/47 窗集

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户指令「先执行只读地评估可行性，可行再跑残差校正」
- **实际动作**：只读阅读 `scripts/rr_quality_residual_corrector.py`（571 行，含 `curve_quality_features`、
  `safe_adjust_target`、`build_feature_frame`、CV 逻辑），并逐项核对 49 窗集的可满足性。
  **未运行校正器、未生成结果、未修改项目数据。**
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/07_残差校正迁移评估/01_可行性评估_20260922.md` | 新增，可行性评估 |

- **方法机制（读代码所得，关键点）**：
  - 训练目标 `target = clip(truth_count − peaks, −1, +1)` → 三分类；
  - **它最多只把预测计数改 ±1 次呼吸**，天然是小幅修正器；
  - **训练目标直接来自人工真值** → 属**带监督的后处理校正**，非无监督方法（OOF CV 下合法）；
  - 硬编码读取 `{input_root}/{video_id}/{prefix}_curve.csv` 与 `{prefix}_summary.csv`。
- **可行性核对结果**：

| 依赖 | 73 集 | 49/47 窗集 | 判定 |
|---|---|---|---|
| 曲线 CSV 内容 | `{vid}/paper_repro_curve.csv` | `curves/anchored49/{variant}/{vid}.csv` | ✅ 兼容，路径不同 |
| `smoothed_norm` / `is_peak` | ✅ | ✅ | ✅ |
| `left/right_missing_raw` | ✅ | ❌ 缺列 | ⚠️ 丢失 2 特征 |
| `fusion_source` | ✅ | ❌ 缺列 | ⚠️ 丢失 1 特征 |
| summary 特征列 | **67 列** | `paired_predictions.csv` 仅 **20 列** | ⚠️ **大幅缩水** |
| `truth_count` / `peaks` | ✅ | ✅ 可构造 | ✅ |
| 样本量 | 73 | **47** | ⚠️ 偏小 |

- **结论**：**有条件可行**。推荐**方案 3（纯文件布局适配）**：把曲线复制到 `deepseek/` 下临时根目录，
  summary 用 `paired_predictions.csv`，`--input-root` 指向该目录 → **不改任何项目代码**。
  但特征集与 73 集**不同构**，属"缩水版复现"，不可宣称"复现 73 集的方法"。
- **发现的三个方法学限制（必须写入论文或事前说明）**：
  1. **带监督**：训练目标来自真值，部署时无真值 → 只能用于离线修正/有标注场景；
  2. **最多改 ±1**：提升幅度天然受限（73 集 nested 仅 +0.008）；
  3. **视频重叠**：49 窗视频全部是 73 视频的子集，**禁止"73 训练→49 测试"**（同视频泄漏）；
     只能做 47 窗内 OOF CV；剔除重叠后训练集仅剩 24 样本，**不可行**。
- **建议的事前判据（先定再看数字）**：
  基线取 **r20 固定**（并附 F1G1P1 对照）；报固定阈值 OOF 与 **nested 阈值 CV** 两者；
  **nested 的 ΔMAE 95% 区间不跨 0 → 方向可行；跨 0 → 判定该方向在 30 秒窗上不成立并停止投入**。
- **诚实预判**：nested 区间跨 0 的概率**较高**（47 样本 + 特征缩水）。该实验值得跑，
  但**不应期待它救活论文**；其价值在于给出"残差校正方向去留"的终局裁决。
- **采用决定**：**仅评估，未执行**。是否开跑由用户决定。
- **后续**：等用户确认开跑后，按 §6 预设口径执行，产物全部落在 `deepseek/07_残差校正迁移评估/`。

---

## D009 执行残差校正迁移实验：**裁决不成立，建议停止该方向**

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户指令「开跑吧」
- **事前判据（D008 §6 预先固定）**：nested 阈值 CV 的 **ΔMAE 95% 区间不跨 0 → 可行；跨 0 → 不成立**
- **执行口径（已知偏差须记明）**：用户指定的基线 **r20 固定没有保存曲线 CSV**
  （`common_cached_r20/` 仅有表格），校正器无法为其算曲线特征。
  故改用**同一 r20 输入、且保存了 49 条曲线**的两个消融变体作基底：
  **F1G1P1**（0.890902）与 **F1G0P1**（0.894273）。
  两者与 r20 固定（0.872816）的预测计数平均差 0.388 / 0.408，**不能宣称等价**。
- **改动文件（全部在 deepseek/ 内，未动项目文件）**：

| 路径 | 说明 |
|---|---|
| `deepseek/07_残差校正迁移评估/setup_inputs.py` | 新增，搭建输入目录（47 样本 + 47 曲线 ×2 基底） |
| `deepseek/07_残差校正迁移评估/02_执行结果与裁决_20260922.md` | 新增，执行结果与裁决 |
| `deepseek/07_残差校正迁移评估/run_F1G1P1/`、`run_F1G0P1/` | 运行产物（metrics / predictions / nested / bootstrap CI） |

- **结果（47 窗）**：

| 基底 | 口径 | R² | MAE | 完全计数 |
|---|---|---:|---:|---:|
| F1G1P1 | baseline | 0.890902 | 2.978055 | 9/47 |
| F1G1P1 | 固定阈值 OOF | 0.898301 | 2.935567 | **8/47** |
| F1G1P1 | 阈值网格最佳（挑过） | 0.900988 | 2.850541 | 9/47 |
| **F1G1P1** | **nested CV（诚实）** | **0.894272** | **2.935525** | 9/47 |
| F1G0P1 | baseline | 0.894273 | 2.935488 | 9/47 |
| **F1G0P1** | **nested CV（诚实）** | **0.894272** | **2.935525** | 9/47 |

  - **nested ΔR²**：F1G1P1 **+0.00337**；F1G0P1 **−0.000001（完全为零）**
  - **配对 bootstrap（5000 次，按窗）**：ΔMAE = **−0.042488**，95% CI **`[−0.255243, +0.170231]`** → **跨 0**
    （ΔR² 区间 `[−0.004022, +0.022797]`、ΔRMSE `[−0.353343, +0.070332]`、Δ完全计数 `[−4, +2]`，均跨 0）
  - **固定阈值版本破坏正确预测**：7 次修正中改善 4 / **变差 3**；**摧毁 2 个原本完全正确的窗**
    （`bs211063` 32→31、`zs200751` 29→28），完全计数 **9→8**
- **与 73 集对比**：nested 增益 **+0.0083（73 短片）→ +0.0034 / 0.0000（47 窗）**，随任务变难而消失
- **采用决定**：**按事前判据判定该方向在 30 秒窗集上不成立，建议停止投入**。
  - 该方向**不能**作为论文贡献；
  - 73 集上的 +0.0083 只能作为"短片集上的弱增益"说明，不能作主贡献。
- **连带影响**：D007 提出的"以残差校正支撑方法主张"被本次数据否定。
  **路线 A（失败分析）与路线 B（方法论文）均已被负结果关闭，只剩路线 C（评价方法）**：
  以"事件级评价协议 + 计数指标掩盖事件错误"为主贡献——该主张不依赖"优于他法"，
  故不受 D004–D009 全部负结果影响。
- **合规声明**：未改动任何项目文件；项目脚本仅只读调用；未重跑模型/推理；
  使用消融已保存的 r20 输入与曲线；随机种子为脚本默认（42/4）。
- **后续**：建议转入路线 C，先做"路线 C 的可行性论证"（需用户确认）。

---

## D010 审阅旧手稿与 Q2+ 路线图：**第三次更正（D007 撤销）** + 旧线评估

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户提供 `docs/thermal_rr_manuscript_draft.md`（sha256 `1e18bd69…`）与
  `.../paper_literature_q2_plus_innovation_roadmap.md`（sha256 `78a4868c…`），要求评估"不行再转入路线 C"
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/06_B73来源核查/02_B73P真值调整最终结论_20260922.md` | 新增，更正 D007 |
| `deepseek/08_旧手稿与路线图评估/01_旧研究线评估与裁决_20260922.md` | 新增，旧线评估 |

### 🔴 更正 D007：D006 原本是对的，D007 的"更正"错了

- 项目内存在**两份 73 集 summary**：
  `innovation_repro_summary.csv`（R²=**0.928992**，**未调整**真值）与
  `paper_repro_summary.csv`（R²=**0.946595** = B73-P，**已调整**真值）。
- 两者 `truth_count` 差异**恰好 4 个视频**（`210994`/`211049`/`ns196527`/`ns211117`），
  **四个全部被改成与算法预测完全一致，误差全部归零**。
- **D007 的检查是循环论证**：我拿"被调整后的 73 表"当"未调整基准"去比对，必然一致。
  正确基准是更早的 `innovation_repro_summary.csv`。**教训：判断真值是否被改，必须与更早的时间快照比对。**
- **连带解释 D006 悬案**：49 窗表 `short_clip_reference_rr_bpm_not_window_truth` 与 73 表 `rr` 的 2 行不一致中，
  `ns196527` 正是被调整的 4 个视频之一（旧计数 10 → 新 11，66.7 → 73.3），
  **是同一次真值调整在两表中未同步**，不是"两个并行版本"。
  （另一处 `bs170227` 不在这 4 个之中，成因待追溯。）
- **已核实的干净文件**：`paper_repro_signal_aware_safe_policy_predictions.csv` 与
  `paper_repro_quality_residual_predictions.csv` 的真值 vs innovation **0 处不同** → **旧研究线建立在未调整真值上**。

### 旧研究线评估（`thermal_rr_manuscript_draft.md` + Q2+ roadmap）

**这条线比总记录呈现的强得多，且是干净的**，但过不了它自己设的 Q2 门槛。

- **内部结果（已独立复算，均用未调整真值）**：

| 方法 | R² | MAE | exact |
|---|---:|---:|---:|
| default（innovation，诚实基线） | 0.928992 | 1.9713 | 49/73 |
| quality-aware residual correction（固定 OOF） | 0.942885 | 1.6187 | 53/73 |
| signal-consensus 补充 | 0.946345 | 1.4955 | 55/73 |
| signal-aware residual（固定 OOF） | 0.949482 | 1.3631 | 58/73 |
| **safe gate（固定 OOF）** | **0.951993** | **1.2884** | **59/73** |
| signal-aware（prefix-group） | 0.909734 | 1.8243 | 54/73（≥2 误差 2） |
| **safe gate（prefix-group）** | **0.949883** | **1.3569** | **58/73（≥2 误差 0）** |

  → **default → safe gate = +0.0230 R²、MAE −0.683、exact +10**。
  ⚠️ 但 `..._quality_residual_bootstrap_ci.csv` 显示 **residual correction 单独 vs default 的 ΔR²=+0.0139 `[−0.0059,+0.0411]` 跨 0** ——显著性来自多级叠加。
  ⚠️ 手稿摘要的 safe-gate-vs-default CI `[0.0020, 0.0545]` **源文件未定位，未独立复算**。
- **跨场（久福 94 clips，provisional）**：baseline **−0.8628** → **P2g 相对伪彩集合 0.4973**
  （vs offline context ΔR²=+0.2687，CI `[0.0899,0.6365]` 不跨 0）。**这是全项目唯一的大幅跨场改进。**
- **辅助线**：Bland–Altman LoA 13.400→11.009 bpm；选择性报告（strict auto n=13 / coverage 0.178 / R²=0.9999）；
  conformal 覆盖率 0.904 / 宽度 11.79 bpm；89 特征块消融；`ns` prefix-group 全方法不过门槛；双盲 A/B 包**已备好但从未执行**。
- **三条致命问题**：
  1. 根本约束未变（73 视频 / 单牧场 / 无身份·日期·相机元数据；roadmap 自评 `Q2 readiness = not_ready`）；
  2. 增益本质是"±1 次呼吸修正"，而基线 **73/73 都已在 1 次以内**；
  3. **5 级流水线（corrector→consensus→signal-aware→safe gate→rollback）全部在同一批 73 视频上设计**，
     `nested CV` **只嵌套阈值、未嵌套架构搜索**，prefix-group 只是文件名前缀代理 → **+0.023 无法排除选择偏差**。
- **与当前记录的冲突（必须解决）**：久福 94-clip / 0.4973（相对伪彩）vs 271 窗 / 0.4196（RF 绝对代理）；
  两条线模型、真值、样本都不同，**数字不可比**；且**总记录完全没有覆盖旧线**。
- **采用决定**：**不建议直接改写旧稿投稿**（过不了自设 Q2 gate）。
- **建议的下一步（用户决定）**：
  1. **先做一件有界测试**：把 **P2g 相对伪彩集合**接到**当前 271 窗 R2 参考**上测一次。
     **事前判据**：R² 显著高于 0.4196 且配对 CI 不跨 0 → 论文有了真正的跨场方法贡献；否则 → 转路线 C。
  2. 无论走哪条路，先**在总记录登记 B73-P 使用调整真值**，并把旧线补进总记录；
  3. 排查还有哪些 `paper_repro_*` 继承了这个调整真值。

---

## D011 真值污染排查 + 总记录更正条目起草 + P2g 测试可行性

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户指令「排查一遍还有哪些文件受污染，帮你起草总记录的更正条目，做 P2g 那个有界测试」
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/09_真值污染排查/scan_contaminated_truth.py` | 新增，可复现扫描脚本 |
| `deepseek/09_真值污染排查/01_污染排查报告_20260922.md` | 新增，污染范围报告 |
| `deepseek/10_总记录更正条目/01_建议条目_N070_C032_20260922.md` | 新增，供粘贴的 N070/C032 草稿 |
| `deepseek/11_P2g测试/01_可行性评估与方案_20260922.md` | 新增，P2g 方案（**更正成本估计**） |

### 任务 A：污染排查（已完成）

- 以 4 个被改视频（`210994`/`211049`/`ns196527`/`ns211117`）为指纹扫描
  `Dataset_new/72video`、`external_al_images_single_reference`、`Experiment/` 下的 CSV。
- **结果：ADJUSTED 34 个 / clean 76 个 / MIXED 0 个**（全有或全无 → 一次性写入源表后向下游派生）。
- **污染根**：`Dataset_new/72video/video/temperature_curves.csv`（73 视频参考清单 D02）与 `paper_repro_summary.csv`。
- ⚠️ **`Experiment/01_同口径消融/runs/20260909_v1/paired_predictions.csv` 部分受影响**：
  `internal73` 的 32 行（4 视频 × 8 变体）全取调整值 → **E040 的 73 列结果建立在调整真值上**；
  `anchored49` 使用 49 窗人工计数，**不受影响** → **D004/D006/D009 所用的 49/47 窗结论依然成立**。
- ✅ **旧研究线（safe gate / 残差校正 / signal-consensus / calibration-free P2g / meta*）全部 clean**
  （其 `truth_count` 与 `innovation_repro_summary.csv` 0 处不同）。

### 任务 B：总记录更正条目（草稿已出，未改总记录）

- 按总记录自身格式起草：**新节点 N070** + **新更改 C032/C033** + §1.2 第 1 条改写 + 新增第 11 条 +
  §3.2 B73-P 行作废标注 + 页首字段更新建议。
- **未修改 `docs/cow_rr_project_log.md`**（Codex 侧唯一总账），由用户决定是否粘贴。

### 任务 C：P2g 测试（**勘察完成，尚未执行**）

- ⚠️ **更正 D010 的成本估计**：这不是"重打分"，而是**像素级重算**。理由：
  1. 旧外部队列（94 clips，24 个时间戳）与当前 271 窗（271 个时间戳）**源视频交集为 0**
     （271 窗按构造排除了 `split_all_use` 开发场次）→ 无法复用已算信号；
  2. **271 窗目录没有保存帧图像**，`curve.csv` 也**不含相对伪彩通道列**；
  3. P2g 的 `extract_video_signals()` 需要 `{prefix}_temperatures.csv` 的 ROI 坐标 **+ 帧图像**。
- ✅ **271/271 源视频齐全（3.10 GB）** → 技术上可行。
- **修正工作量**：编码半天 + 计算 1–2 小时（参照 E051 外测 271 窗约 80 分钟）。
- **需用户确认一个设计选择**：与当前外测**同 ROI（用 `radius_table.csv`，推荐 A）** 还是固定 r20（B）。
  推荐 A——本次要回答"相对伪彩信号是否比 RF 绝对温度代理更能跨场"，应固定 ROI、只换信号。
- **事前判据已定**：在 R2 的 173 配对窗上，**ΔR² 的 95% CI（按牛号聚类、5000 次）不跨 0 且为正 → 方向成立**；
  跨 0 → 转路线 C。**不重排通道、不调门限、不按外部结果选参。**
- **诚实预期**：旧队列 baseline 是 −0.86（极差），当前 RF 代理已有 0.4196，P2g 要打败的对手强得多；
  点估计更高但 CI 跨 0 的可能性较大。**该测试的定位是"判定"，不是"救场"。**

- **采用决定**：任务 A/B 完成；任务 C **等用户确认 §5 选项后执行**（会先做 3–5 窗试点再跑全量）。
- **合规**：**未修改任何项目文件**；全部产物在 `deepseek/` 内。

---

## D012 P2g 测试执行（选项 A）：通道提取器已建成并通过位精确验证，全量提取后台运行中

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：用户指令「按 A 开跑」
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/11_P2g测试/extract_p2g_signals.py` | 新增，通道提取器 |
| `deepseek/11_P2g测试/score_p2g.py` | 新增，评分脚本（草稿，待补齐对照） |
| `deepseek/11_P2g测试/02_执行状态_20260922.md` | 新增，执行状态 |
| `deepseek/11_P2g测试/.gitignore` | 新增，排除 `signals/` 中间数据 |

- **关键设计（选项 A）**：
  - 帧重建：导入项目**活动模块** `frames_from_anchor`（`validate_raw_pipeline_internal49.py`）
    + `sample_map`（`timestamp_adapter.py`），解码源视频后 **JPEG q95 编码**；
  - **位一致性验证**：重建帧 SHA256 与项目 `frame_hashes.csv` **逐帧比对**；
  - ROI：用 `temperatures.csv` 的**逐帧 `adaptive_roi_radius`**（与当前外测一致）；
  - 通道：8 个（gray/blue/green/red/hue/lab_a/lab_b/red_minus_blue），圆形掩膜，**逐行复刻**
    `build_rr_calibration_free_thermal_index.roi_means()`。
- **试点结果（3 窗）**：**783 / 783 帧哈希逐位一致**（`hash_ok=261, hash_bad=0` × 3）
  → 帧重建与当时外测运行完全相同。
  通道合理性：`corr(left_gray, left_temp)=0.9989`、`corr(right_gray, right_temp)=0.9988`。
- **全量提取**：待处理 **218 窗**（有 `map.csv` 的非弃权窗；53 窗为时间轴弃权、无采样映射），
  后台运行中，每窗 261 帧、哈希逐位一致；预计 1–1.5 小时。
- **评分设定（已定，未执行）**：通道与显著度用**内部冻结值**
  （7 成员 `gray:direct;lab_b:direct;red_minus_blue:inverted;blue:direct;lab_b:inverted;lab_a:inverted;red_minus_blue:direct`，
  prominence=0.05）；组合规则 = 逐成员 `fused_curve`+`estimate_curve` 后取 **7 个 RR 的均值**；
  config = `derive_config(内部 73 集 summary, fps=8.7)`；参考 = R2 20260918（216 完整 / 173 配对）；
  对照 = RF 绝对代理 E055（R²=0.419629、MAE=5.156069）；按牛号聚类配对 bootstrap 5000 次。
- **⚠️ 必须记录的设计事实**：P2g 的 **duration gate 在 271 窗上恒为激活**，
  故本次等价于"**纯相对伪彩集合 vs RF 绝对温度代理**"，duration gate 不构成差异来源。
- **事前判据（不变）**：ΔR² 的 95% CI 不跨 0 且为正 → 方向成立；跨 0 → 转路线 C。
- **合规**：未修改任何项目文件；项目模块仅只读导入；`signals/` 中间数据（约 20 MB）已排除出仓库。

---

## D013 P2g 有界测试执行完毕：**裁决不成立，转入路线 C**

- **日期**：2026-09-22，Asia/Shanghai
- **来源**：D012 后台提取完成（exit 0）后执行评分
- **事前判据（D010 固定）**：ΔR² 的 95% CI（按牛号聚类）不跨 0 且为正 → 成立；跨 0 → 转路线 C
- **改动文件**：

| 路径 | 说明 |
|---|---|
| `deepseek/11_P2g测试/score_p2g.py` | 更新为终版（配对对照 + 聚类 bootstrap） |
| `deepseek/11_P2g测试/03_执行结果与裁决_20260922.md` | 新增，结果与裁决 |
| `deepseek/11_P2g测试/p2g_vs_rf_paired_predictions.csv`、`p2g_vs_rf_bootstrap.csv` | 新增，产物 |

- **执行链验证**：
  - 通道提取 **215 窗 / 56,115 帧，SHA256 与项目 `frame_hashes.csv` 逐位一致，0 不一致**；
  - 173 个 R2 配对窗**全部给出有限预测（0 缺失）**；
  - 规则为内部冻结值（7 成员 + prominence 0.05），**未重排通道、未调参**；
  - 统计：按牛号聚类配对 bootstrap 5000 次、126 个牛号、seed 20260922。
- **结果（同一批 173 配对窗，R2 20260918 参考）**：

| 指标 | RF 绝对代理（E055） | P2g 相对伪彩 | Δ |
|---|---:|---:|---:|
| RR R² | 0.419629 | **0.455659** | **+0.0360** |
| RR MAE | **5.1561** | 5.6227 | +0.4666（更差） |
| RR RMSE | 7.8571 | **7.6093** | −0.2478 |
| **完全计数** | **38/173** | **17/173** | **−21** |
| 误差 ≤1 次 | **77/173** | 66/173 | −11 |

  - 配对 bootstrap：ΔR²=**+0.0377 95%CI `[−0.0470, +0.1378]`（跨 0）**；
    ΔMAE=+0.4712 `[−0.0607, +0.9997]`；ΔRMSE=−0.2344 `[−0.8064, +0.3779]` —— **三项全部跨 0**。
- **采用决定**：**按事前判据判定该方向不成立，转入路线 C。**
- **关键解读**：旧线 P2g 的"巨大提升"（−0.8628 → 0.4973）**很大部分来自对手太弱**；
  换成当前已可用的 RF 代理（0.4196）后优势消失，且 **P2g 计数指标全面更差**（完全计数 17 vs 38）。
  旧线"最有希望的那张牌"在当前可信参考上**没有兑现**。
- **路线状态**：A（失败分析）❌、B（方法论文）❌ **D006+D009+D013 三重否定**、**C（评价方法）✅ 唯一剩下**
- **后续建议**：做**路线 C 可行性论证**（只读、零模型）：独立复算"9/38 完全一致窗全部有未匹配事件"、
  事件数分母口径、`Npred−Nref = FP−FN` 逐窗是否成立；并盘点文献中事件级指标的覆盖情况。
- **合规**：未改动任何项目文件；项目模块仅只读导入；未重跑 YOLO/RF/RR。

---

## 节点模板（后续复制使用）

```markdown
## D### 节点名称

- **日期**：YYYY-MM-DD，Asia/Shanghai
- **来源**：用户消息 / 文件核验 / 实验
- **需求**：
- **实际动作**：
- **改动文件**：

| 路径 | 旧值 → 新值 / 说明 |
|---|---|
|  |  |

- **结果与验证**：
- **采用决定**：保留 / 回退 / 候选 / 待核实 / 暂停；理由
- **备注/待更改**：保留用户原备注
- **后续**：关联项目总记录 T/C 编号
```
