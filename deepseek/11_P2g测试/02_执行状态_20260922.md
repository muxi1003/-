# P2g 测试执行状态（进行中）

- **更新**：2026-09-22，Asia/Shanghai（节点 `D012`）
- **状态**：🟡 **通道提取正在后台运行**，评分待提取完成后执行

---

## 1. 已完成：通道提取器 + 位精确验证 ✅

**脚本**：`extract_p2g_signals.py`

### 关键设计（选项 A）

| 项 | 设定 |
|---|---|
| 帧重建 | 导入项目活动模块 `frames_from_anchor` + `sample_map`，解码源视频并 JPEG q95 编码 |
| **位一致性验证** | 重建帧的 SHA256 与项目记录的 `frame_hashes.csv` **逐帧比对** |
| ROI | **用 `temperatures.csv` 的逐帧 `adaptive_roi_radius`（选项 A）**，与当前外测完全一致 |
| 通道 | 8 个（gray/blue/green/red/hue/lab_a/lab_b/red_minus_blue），圆形 ROI 掩膜，逐行复刻 `roi_means()` |

### 试点结果（3 窗）

```
[1/3] jiufu_336c6a37c1e8bf_first30  frames=261  hash_ok=261  hash_bad=0
[2/3] jiufu_3daca4f99093f5_first30  frames=261  hash_ok=261  hash_bad=0
[3/3] jiufu_aed96a8cd6dde3_first30  frames=261  hash_ok=261  hash_bad=0
```

**783 / 783 帧哈希逐位一致 → 帧重建与当时外测运行完全相同。**

**通道合理性**：`corr(left_gray, left_temp) = 0.9989`、`corr(right_gray, right_temp) = 0.9988`
（同一 ROI 的两种度量，预期强相关）；缺失帧数与管线自身无检测帧一致。

---

## 2. 进行中：全量提取

- 待处理窗口 **218**（有 `map.csv` 的非弃权窗；另有 53 窗为时间轴弃权，无采样映射）
- 每窗 261 帧，全部 `hash_ok=261, hash_bad=0`
- 产物：`signals/{window_id}_calibration_free_signals.csv`（约 100 KB/窗）
- **预计 1–1.5 小时**

---

## 3. 待执行：评分

**脚本**：`score_p2g.py`（已起草，提取完成后需补齐对照部分）

| 项 | 设定 |
|---|---|
| 通道与显著度 | **内部冻结值**：7 成员 `gray:direct;lab_b:direct;red_minus_blue:inverted;blue:direct;lab_b:inverted;lab_a:inverted;red_minus_blue:direct`，prominence=0.05 |
| 组合规则 | 逐成员 `fused_curve` + `estimate_curve`，取 **7 个 RR 的均值**（复刻 `selected_member_predictions`） |
| config | `p2g.derive_config(内部 73 集 summary, fps=8.7)`（与原始运行同源） |
| 参考 | R2 20260918 状态修订版（久福 216 完整参考 / 173 计数配对窗） |
| 对照 | 当前 RF 绝对温度代理 E055：**R²=0.419629、MAE=5.156069** |
| 统计 | 按牛号聚类的配对 bootstrap，5000 次，固定种子 |

**⚠️ 一个必须记录的设计事实**：P2g 的 **duration gate 在 271 窗上恒为激活**
（所有窗 `raw_duration_seconds` 均 ≥ 门限），因此本次比较**等价于"纯相对伪彩集合 vs RF 绝对温度代理"**，
duration gate 不构成差异来源。

---

## 4. 事前判据（D010 已定，不变）

**ΔR² 的 95% CI（按牛号聚类）不跨 0 且为正 → 相对伪彩方向成立；跨 0 → 转路线 C。**

**不重排通道、不调门限、不按外部结果选参。**

---

## 5. 合规

- **未修改任何项目文件**；项目模块仅只读导入
- 产物全部在 `deepseek/11_P2g测试/`
- `signals/` 为中间数据（约 20 MB），已通过本地 `.gitignore` 排除出仓库
