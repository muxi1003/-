# B73 来源核查与 73 集结果谱系

- **日期**：2026-09-22，Asia/Shanghai（节点 `D007`）
- **触发**：用户提供 Codex 会话截图（显示"默认流程 0.928992 / 质量感知残差校正 5折CV 0.946624"），并要求核清 B73-P 的来源
- **性质**：只读核算。未修改任何项目数据文件、算法或人工计数。

---

## ⚠️ 1. 更正我在 D006 的错误表述

我在 D006 写的是：

> "**B73-P = 0.946595 是调整真值后的结果**，不是评估结果"

**这个说法不成立，我收回。** 核查证据：

| 检查 | 结果 |
|---|---|
| `paper_repro_summary.csv` 的 `truth_count` vs 73 表 `temperature_curves.csv` 的 `breath_count` | **73 / 73 完全一致** |
| 两者 `duration_seconds` | **73 / 73 完全一致** |

**即 B73-P 使用的真值与 73 表逐行相同，没有发现改真值的痕迹。**

我把你"根据算法预测结果调整了几个呼吸频率"这句话，直接推断成了"改真值"，
这是**超出证据的推断**。实际发生的是**调算法规则**，不是改真值（见 §3）。这个区别很重要，特此更正。

---

## 2. B73-P 的真实来源（已精确复现）

**B73-P 可由 `Dataset_new/72video/al_images/paper_repro_summary.csv` 完整复现**：

| 指标 | 我用 summary 复算 | `paper_repro_metrics.csv`（B73-P） |
|---|---:|---:|
| RR R² | **0.946595** | 0.9465953232567761 |
| RR MAE | **1.691728** | 1.691728363357693 |
| RR RMSE | **3.006423** | 3.0064230811498973 |
| 完全计数 | **51/73** | 51 |

→ **B73-P = `paper_repro_rr.py` 在这套配置下跑出来的自动化结果**，不是手工填的数字。

### 2.1 这份 summary 的一个显著特征

| 字段 | 取值 |
|---|---|
| `status` | **`retuned`（73/73 全部）** |
| `adaptive_peak_retuning` | **True（73/73）** |
| `peak_retune_rule` | `none` 64 · `dense_peak_count` 6 · `short_sparse_peak_count_edge` 3 |
| `retune_target_peaks` | 仅 6 行有值，**全部 = 10.0** |

**关于"6 行目标峰数都是 10、且真值也都是 10"**：
`truth_count = 10` 在 73 段里占 **44/73（60%）**，所以这 6 行命中并不构成证据。
**不能据此宣称"目标峰数抄了真值"。**

### 2.2 真正的问题在哪

项目总记录 §3.5 自己已经写明（原文）：

> "**73 的 P 含开发集专用目标峰数 10 规则，禁止直接迁移为独立测试方法。**"

也就是说：**这套自适应峰重调规则是在这 73 段上开发出来的。**
所以 B73-P 的问题不是"改真值"，而是 **"在评测集上开发并调参"** ——
这仍是污染，但性质轻得多，且**项目自己已经登记过**。

---

## 3. 73 集完整结果谱系（按阈值选择方式的诚实度排序）

数据来源：`docs/thermal_rr_innovation_plan.md`、
`paper_repro_quality_residual_metrics.csv`、`paper_repro_quality_residual_nested_metrics.csv`。

| 方法 | RR R² | MAE | 完全计数 | 阈值选择方式 | 诚实度 |
|---|---:|---:|---:|---|---|
| **当前默认流程（= B73-I）** | **0.928992** | 1.9713 | 49/73 | — | 基线 |
| 质量感知残差校正，固定阈值 OOF | 0.942885 | 1.6187 | 53/73 | 预先固定 | 较诚实 |
| 质量感知残差校正，leave-one-prefix-group-out | 0.932500 | 1.8577 | 51/73 | 组外 | **最接近换批次** |
| **质量感知残差校正，nested 阈值选择 CV** | **0.937336** | 1.7757 | 51/73 | 嵌套 | **最诚实** |
| 质量感知残差校正，组内选阈值 | 0.936263 | 1.7305 | 53/73 | 组内 | 中等 |
| 质量感知残差校正，**阈值网格最佳 OOF** | 0.945944 | 1.5017 | 55/73 | **在全量上挑最优阈值** | ⚠️ 乐观 |
| **去掉端点间隔（变体）** | **0.946624** | 1.5388 | 54/73 | 同上 | ⚠️ **截图引用的就是这个** |
| truth-calibrated 上限 | 0.999655 | 0.1389 | 73/73 | — | ⚠️ 文档已标注为"上限分析" |
| **B73-P（`paper_repro_summary.csv`）** | **0.946595** | 1.6917 | 51/73 | 开发集调峰规则 | ⚠️ 开发集调参 |

### 3.1 关于截图里的 0.946624

**它不是"质量感知残差校正"的主结果**，而是
`docs/thermal_rr_innovation_plan.md` 中"**去掉端点间隔**"这个**变体**。

该文档同一节里更诚实的数字是：

- 固定阈值 OOF = **0.942885**
- nested 阈值选择 CV = **0.937336** ← **应该用这个**
- leave-one-prefix-group-out = **0.932500**
- 阈值网格最佳 OOF = 0.945944 ← **这个挑过阈值，不能作主结果**

**如果论文引用 0.946624 或 0.945944，等于选了最有利的变体**，审稿人会问阈值是怎么定的。

### 3.2 一个重要的数值确认

`paper_repro_quality_residual_metrics.csv` 的第一行是：

```
baseline_default,73,73,0.9289923446304912,...
```

**与 B73-I 的 0.9289923446304912 完全相同。**

→ **这从数值上确认了你的说法：B73-I = `paper_repro_rr.py` 的"当前默认流程"= 忠实复现基线。** ✅

---

## 4. 对本项目最重要的一个新发现

**"质量感知残差校正"是本项目目前唯一一个拿到正结果的候选方法。**

在**诚实的验证口径**下：

| 口径 | R² | 相对默认 |
|---|---:|---:|
| 默认流程（B73-I） | 0.928992 | — |
| 分组外推（leave-one-prefix-group-out） | 0.932500 | **+0.0035** |
| **嵌套 CV（nested threshold CV）** | **0.937336** | **+0.0083** |
| 固定阈值 OOF | 0.942885 | +0.0139 |

**即：一个真实的、有交叉验证支撑的 +0.008 ~ +0.014 提升。**

这**不惊人，但它是本项目里第一个"方向为正、且有 CV 支撑"的方法结果**——
对比之下，跨牧场（0.42）、r20 对照（区间跨 0）都是负的或零。

`docs/thermal_rr_innovation_plan.md` 自己也做了分组验证并如实记录：
> "…该创新方向有跨组潜力。但 `bs` 组下降、`ns` 组无改善，说明还不能宣称已经解决泛化问题。
> 二区以上投稿时，必须补充真实 cow_id/date/scene 分组或外部测试集。"

---

## 5. 修正后的完整判断

| 事项 | 修正后的结论 |
|---|---|
| B73-I 是不是忠实复现 | ✅ **是**（数值确认 = `baseline_default`） |
| B73-P 是不是改真值 | ❌ **不是**（真值 73/73 与 73 表一致）。但在**开发集上调过峰规则** |
| B73-P 能不能当基线 | ❌ 不能——不是因为改真值，而是因为**规则在评测集上开发** |
| 0.946624 能不能写 | ❌ 不能作主结果（是挑过阈值的变体） |
| 有没有正面结果 | ✅ **有**：质量感知残差校正，诚实口径 **0.9373**（nested CV） |
| 跨牧场/原文对照 | ❌ 仍不成立（D004 / D006） |

---

## 6. 建议的下一步

| 优先 | 任务 | 理由 |
|---|---|---|
| **1** | **把"质量感知残差校正"的诚实数字（nested CV 0.9373 / group-out 0.9325）核实并固化** | 这是目前唯一的正结果，值得先钉死 |
| **2** | 在 **49/47 窗集**上跑同一套残差校正 | 若在 30 秒窗上也有 +0.01 级别的稳定提升，论文就有主张了 |
| 3 | 在总记录登记 B73-P 的适用范围（"开发集调峰规则，不作基线"） | 更正我 D006 的表述，并让后续引用有据 |
| 4 | `bs170227` 短片参考来源追溯 | 影响小 |

**我特别建议第 2 项。** 理由：残差校正目前只在 73 短片集上验证过；
而 73 短片集有"短片参考方差大"的问题（见 D005）。**如果它在 30 秒窗集上同样有效，
那就同时解决了"方法主张"和"数据集偏弱"两个问题。**
